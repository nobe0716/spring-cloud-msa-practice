server:
  port: 8888

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/IdeaProjects/cop/cop-springcloudmsa-config
#          uri: file://${user.home}/IdeaProjects/cop/multi-modules
#          search-paths: ./conf
#          default-label: main

