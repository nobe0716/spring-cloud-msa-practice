server:
  port: 8888

spring:
  application:
    name: config-service
  profile:
    active: native
  cloud:
    config:
      native:
        search-locations: file://${user.home}/IdeaProjects/cop/spring-cloud-msa-config
      server:
        git:
          uri: https://github.com/nobe0716/spring-cloud-msa-config
          default-label: main
